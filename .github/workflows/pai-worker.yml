# placeholder - will be replaced immediately to force workflow re-registration
name: PAI Worker Placeholder
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo placeholder
